from Character import Character
from Monster import Monster
from Hero import Hero


def add_char(type):
	if type == "PC":
		#create new PC Hero
	elif type == "Mon":
		#create new Monster
	elif type == "NPC":
		#createnew NPC Hero
	else:
		#throw error
